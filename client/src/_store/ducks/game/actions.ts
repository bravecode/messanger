import { createAction } from '@reduxjs/toolkit';

// Action - Fetch Game Info

// Action - Make Game Action
export const makeGameAction = createAction('game:makeGameAction');

// Action - Update Game State
export const updateGameState = createAction('game:updateGameState');